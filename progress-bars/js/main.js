/* ============================================================
   Bootstrap. Loaded LAST so all bars are already registered.
   ============================================================ */
window.addEventListener('DOMContentLoaded', () => {
  PBH.start();
});
